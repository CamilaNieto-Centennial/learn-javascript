let lapsCompleted = 0

// Create a function that increments the lapsCompleted variable with one
// Run it three times
function incrementLaps() {
    lapsCompleted++; // lapsCompleted = lapsCompleted + 1
    console.log(lapsCompleted);
}

incrementLaps(); // 1
incrementLaps(); // 2
incrementLaps(); // 3


//console.log(lapsCompleted)